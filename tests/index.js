const test                        = require('tape');
const assert                      = require('yeoman-assert');
const helpers                     = require('yeoman-test');

test('subgenext:app', function(t) {
  t.plan(1);
  t.fail('Test not implemented');
});

test('subgenext:scan --host=x', function(t) {
  t.plan(1);
  t.fail('Test not implemented');
});

test('subgenext:activate foo', function(t) {
  t.plan(1);
  t.fail('Test not implemented');
});

test('subgenext:deactivate foo', function(t) {
  t.plan(1);
  t.fail('Test not implemented');
});
